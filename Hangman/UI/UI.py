from controller.controller import Controller
from repository.Repository import Repository, RepoException
from entities.sentance import Hangman


class UI(object):
    def __init__(self):
        #self.__ctrl = Controller(Repository())
        pass

